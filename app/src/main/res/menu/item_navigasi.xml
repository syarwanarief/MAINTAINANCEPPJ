<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="all">
        <item
            android:id="@+id/beranda"
            android:checked="false"
            android:icon="@drawable/icon_beranda"
            android:title="Beranda" />
        <item
            android:id="@+id/jadwal"
            android:checked="false"
            android:icon="@drawable/icon_tanggal"
            android:title="Cek Jadwal" />

    </group>


    <item
        android:id="@+id/lainnya"
        android:title="Lainnya">
        <menu>
            <item
                android:id="@+id/bantuan"
                android:checked="false"
                android:icon="@drawable/icon_bantuan"
                android:title="Bantuan" />
            <item
                android:id="@+id/tentang"
                android:checked="false"
                android:icon="@drawable/icon_tentang"
                android:title="Tentang" />

        </menu>
    </item>

</menu>